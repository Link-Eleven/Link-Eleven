spring:
  application:
    name: server

server:
  port: ${SERVER_PORT}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: ${SERVER_URL}
  instance:
    hostname: ${SERVER_HOST}
